{
  "name": "veritrust",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18 <21"
  }
}
